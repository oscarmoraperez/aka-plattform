server:
    port: 8082
spring:
    jpa:
        generate-ddl: true
        open-in-view: false
    hibernate:
        ddl-auto: create-drop
    docker:
        compose:
            file: ./docker-compose.yaml
springdoc:
    swagger-ui.path: /catalog-service/swagger-ui.html
