spring:
    h2:
        console:
            enabled: true
            path: /h2/
    datasource:
        driver-class-name: org.h2.Driver
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
    docker:
        compose:
            enabled: false